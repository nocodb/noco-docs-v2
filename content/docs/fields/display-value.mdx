---
title: "Display value"
description: 'This article explains how to set display value for a table & its use.'
tags: ['Fields', 'Display Value']
keywords : ['display value', 'display value in nocoDB', 'display value in nocoDB']
---

A **Display Value** is the key field used to identify a record in NocoDB. It’s the visible name shown across the UI — especially in linked records and spreadsheets — making it easier to recognize and reference records.

While it’s often a unique field (like a name or ID), uniqueness is **not enforced**.

<center>
    <iframe width="560" height="315" src="https://www.youtube.com/embed/GAPAec6zXQ8?start=13" frameBorder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen ></iframe>
</center>

---

## Use of display value

* **In Tables**: Highlighted automatically to help identify the record at a glance
* **In Linked Records**: Appears when linking records across tables

**Examples**
Display value in Actor table:
![display value](/img/v2/fields/display-value.png)

Display value shown when linking records:
![display value- links field](/img/v2/fields/display-value-in-linked-record.png)

---

## Set display value

1. Click the dropdown (🔽) on the desired field
2. Choose `Set as Display Value`

![display value set](/img/v2/fields/set-as-display-value.png)

---

<Callout type="info"> **How is Display Value selected for external tables?** For external tables, NocoDB automatically sets the display value to the first field after the primary key that is not numeric. If all fields are numeric, then the field immediately following the primary key is used instead. </Callout>

<Callout type="info">**Can I change the Display Value?** Yes — just use the dropdown menu on the field and select `Set as Display Value`</Callout>

---